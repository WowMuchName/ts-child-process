{
  "name": "ts-child-process",
  "version": "0.0.2",
  "description": "Asynchronously execute child-processes on the local or a remote machine.",
  "keywords": [
    "ssh",
    "spawn"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^3.0.1"
  },
  "author": "mark.albert@pbforge.com",
  "license": "MIT",
  "dependencies": {
    "@types/ssh2": "^0.5.35",
    "ssh2": "^0.6.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/WowMuchName/ts-child-process.git"
  }
}
